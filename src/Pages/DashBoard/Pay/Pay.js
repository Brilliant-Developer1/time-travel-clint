import { Container, Typography } from '@mui/material';
import React from 'react';

const Pay = () => {
    return (
        <Container>
            <Typography variant='h3' style={{marginBottom:'450px'}}>
            Payment System will Coming Soon
            </Typography>
        </Container>
    );
};

export default Pay;